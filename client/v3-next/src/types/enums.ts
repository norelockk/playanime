export enum LogLevel {
  Info = 'info',
  Step = 'step',
  Error = 'error',
  Debug = 'debug',
  Event = 'event',
  Warning = 'warning',
  Success = 'success',
  Interval = 'interval'
}

export enum UserRole {
  User = 'user',
  Moderator = 'mod',
  Administrator = 'admin'
}

export enum PlayerTattooDestroyReason {
  PlayerDisposed,
}